# Day 128 Slide Plan — 不要迷信 skill

**Title:** Day 128 不要迷信 skill
**Hook:** 別人分享的 skill 不一定有用 — 裝了反而錯更多

**Visual system:**
- Style: `Neon Noir` (cautionary tale, the warning is the message)
- Palette: Danger Signal — black `#18181B`, red `#DC2626` + gold `#FCD34D`, white text
- Typography: bold sans-serif, strong size hierarchy — tagline > key point > hero element > corner signature
- Corner signature: bottom-right, small white monospace `Day 128 · N/6`

| # | Slug | Archetype | Tagline (≤10) | Key point (20–30, takeaway only) | Hero (≤10) |
|---|---|---|---|---|---|
| 1 | cover | The Reveal | `Day 128` | — (cover, exempt) | `94.1% < 96.9%` |
| 2 | counterintuitive | The Chasm | `裝了反而差` | `Google 工程師寫的 skill，通過率 94.1%，沒裝是 96.9%` | `94.1% vs 96.9%` |
| 3 | outdated-doc | The Collision | `過期說明書` | `skill 寫死 API 預設值，沒寫的參數讓 AI 用錯` | `unpublished 漏寫` |
| 4 | live-detect | The Path | `現場偵測` | `沒 skill 的 AI 直接讀 API 規格書，靠當下查補上漏洞` | `直接問 API` |
| 5 | skill-creator | The Spotlight | `先驗證再裝` | `/skill-creator 跑 A/B 測試告訴你裝了到底有沒有變好` | `先 A/B 再裝` |
| 6 | verify-not-believe | The Spotlight | `驗證不迷信` | — (closing insight, exempt) | `Day 128` |

Slides 1 and 6 (cover + closing) are exempt from the key-point rule. Slides 2–5 carry all three text tiers. Archetypes vary across the set (Reveal → Chasm → Collision → Path → Spotlight → Spotlight) — only the closing repeats Spotlight intentionally to land hard.

Read the key points cold:
- 2: states the actual numbers and direction — concrete.
- 3: names the mechanism (`API 預設值`, `沒寫的參數`).
- 4: contrasts with slide 3 — naming the alternative (`API 規格書`, `當下查`).
- 5: states what `/skill-creator` actually does (A/B test).

Each is a complete claim, not a slogan.
