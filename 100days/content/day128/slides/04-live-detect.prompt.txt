Neon Noir. Square 1:1 social media slide. A clean gold path traces from a small AI icon in the lower-left, arcing diagonally upward through the frame to a glowing API endpoint marked "schema 規格書" in the upper-right. Along the path, three waypoint nodes pulse in gold, each labeled with a query: "支援什麼？", "有哪些參數？", "預設值是什麼？". No outdated document blocks the path — the AI talks to the spec directly.

A faint outline of a paper document sits ghosted in the lower-right corner, crossed out with a thin red X — the skill that was bypassed. The journey from AI to live spec is the focal element: clean geometric line, gold waypoints, all rendered against deep black with a faint receding grid. The composition reads left-to-right, bottom-to-top as a successful traversal — no friction, no stale layer.

Chinese text placement — three tiers, clearly differentiated in size:

  TIER 1 — tagline (largest, boldest, gold accent color): "現場偵測"
  anchored to the upper-left, dominant and reading across the top.

  TIER 2 — key point (medium, white text, two tight lines): "沒 skill 的 AI 直接讀 API 規格書，靠當下查補上漏洞"
  positioned in the lower-left third, quieter than the tagline but still highly readable at thumbnail size.

  TIER 3 — hero element (mid-size, red accent): "直接問 API"
  stamped beside the arrival point at the schema endpoint in the upper-right.

In the bottom-right corner, small white monospace text: "Day 128 · 4/6".

Color palette: Danger Signal — black #18181B background, red #DC2626 + gold #FCD34D accents, white text.

Typography: bold sans-serif, strong size hierarchy — tagline > key point > hero element > corner signature.

Flat vector, geometric shapes, high contrast, no photographs, no real people.
