Dark Mode Tech. A horizontal pathway snakes across the frame from left to right, marked by three glowing waypoints. The first waypoint is a small file icon labeled "installed_plugins.json" emitting a blue index card. The path leads to a second waypoint — a stack of plugin folders, each one opened to reveal a "hooks.json" file being lifted out by a lime-green hook. The path then leads to a third waypoint at the right edge: a unified hook table glowing brightly, every entry tagged "source: pluginHook". Two function names hover above the path like signposts. The composition is The Path — a journey from registry to runtime, with mechanism names made visible.

Chinese text placement — three tiers, clearly differentiated in size:

  TIER 1 — tagline (largest, bold white): "啟動時讀檔" anchored to the upper-left, reading downward, dominating the frame as the first eye-catcher.

  TIER 2 — key point (medium, clean white, one or two tight lines, sitting quieter than the tagline but still highly readable at thumbnail size): "啟動時讀 installed_plugins.json，再載每個 plugin 的 hooks.json" positioned across the lower third of the frame as the supporting caption that delivers the actual takeaway.

  TIER 3 — hero element (mid-size, lime-green accent monospace): "RC() + i_K()" floating above the path waypoints like signposts marking the two key functions.

In the bottom-right corner, small lime-green monospace text: "Day 115 · 3/6".

Color palette: Electric Dark — charcoal #1A1A2E, blue #4A90E2, lime #AAFF00, white text.

Typography: bold sans-serif, strong size hierarchy — tagline > key point > hero element > corner signature.

Flat vector, geometric shapes, high contrast, no photographs, no real people.

--ar 1:1
