Dark Mode Tech. Square 1:1 social media slide. A single large JSON file icon partially emerges from the lower half of the frame — its contents are partially visible, partially hidden below a horizon line. The visible portion shows a glowing JSON block with fields like "loop": "ON", "iteration": 12 rendered in blue syntax highlighting. Above the horizon, a bold toggle switch graphic — one side dark (OFF, loop stopped), the other side lime-green (ON, loop running) — hovers in dramatic isolation.

The toggle is the dominant focal point, lit from within in lime green, occupying the upper third of the slide. The JSON file icon below it is larger than the toggle but partially obscured, suggesting its contents are the key — what you can see, you can control. The reveal composition creates tension: the file is right there, readable, transparent.

Chinese text placement — three tiers, clearly differentiated in size:

  TIER 1 — tagline (largest, boldest, lime #AAFF00): "狀態即開關"
  anchored to the upper-left, dominant above the toggle graphic — the first text the eye reads.

  TIER 2 — key point (medium, white, one to two tight lines):
  "ralph-loop.local.json 存在就是 ON，刪掉就是 OFF，隨時可查"
  positioned in the lower third of the frame beneath the JSON icon — quieter than the tagline but still clearly readable at thumbnail size. This delivers the exact mechanism: file exists = loop runs, file gone = loop stops.

  TIER 3 — hero element (mid-size, blue #4A90E2 accent): "一個 JSON"
  floating beside the JSON file icon at the horizon line.

In the bottom-right corner, small white monospace text: "Day 116 · 4/6".

Color palette: Electric Dark — charcoal #1A1A2E background, blue #4A90E2, lime #AAFF00, white text.

Typography: bold sans-serif, strong size hierarchy — tagline > key point > hero element > corner signature.

Flat vector, geometric shapes, high contrast, no photographs, no real people.
