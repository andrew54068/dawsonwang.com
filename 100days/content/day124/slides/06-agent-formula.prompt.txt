Retro Terminal. Square 1:1 social media slide. Two terminal panels side-by-side, contrasting "model alone" vs "model + toolbox", on a dark grid background.

Composition: Left panel shows a single dim card labeled "MODEL" glowing weakly, looking incomplete and inert. An arrow made of lime ASCII chevrons (>>>) points from the left panel to the right panel. Right panel shows the same MODEL card now bright and surrounded by a 2x2 grid of generic tool icons (a small terminal prompt glyph, a file glyph, a globe/browser glyph, and a picture/image glyph), all glowing lime with blue traces connecting them to the central model card. The right panel reads as an operational console; the left panel reads as a dead chip on a shelf.

Chinese text placement — three tiers, clearly differentiated in size:

  TIER 1 — tagline (largest, boldest, lime): "看工具包，不是看模型名字"
  positioned across the upper portion as the primary terminal headline.

  TIER 2 — key point (medium, white, one tight line): "同一個模型，在不同 runtime 裡能做的事可以差很多"
  positioned across the lower third, quieter than the tagline but still highly readable.

  TIER 3 — hero element (mid-size, mixed blue and lime): a small label "模型" beneath the left panel and a slightly larger label "模型 + 工具包" beneath the right panel, making the contrast unmissable.

In the bottom-right corner, small white monospace text: "Day 124 · 6/6" with a tiny lime terminal cursor block beside it.

Color palette: Electric Dark — charcoal #1A1A2E, blue #4A90E2, lime #AAFF00, white text.

Typography: bold sans-serif, strong size hierarchy — tagline > key point > panel labels > corner signature.

Flat vector, geometric shapes, high contrast, no photographs, no real people. Do not render any English labels other than "MODEL" and "Day 124 · 6/6".
