# Day 130 Slide Plan — 我怎麼用 gws 做 Google 表單

**Title:** Day 130 我怎麼用 gws 做 Google 表單
**Hook:** 把素材丟進資料夾，對 Claude Code 說一句話，幾秒拿到能填的 Google 表單編輯 URL

**Visual system:**
- Style: `Clean Minimalist` (workflow/tutorial — calm, precise, trustworthy)
- Palette: Surgical White — white `#FFFFFF` background, orange `#F97316` accent, slate `#0F172A` text
- Typography: bold sans-serif, strong size hierarchy — tagline > key point > hero element > corner signature
- Corner signature: bottom-right, small slate monospace `Day 130 · N/6`

| # | Slug | Archetype | Tagline (≤10) | Key point (20–30, takeaway only) | Hero (≤10) |
|---|---|---|---|---|---|
| 1 | cover | The Spotlight | `Day 130` | — (cover, exempt) | `一句話 = 表單` |
| 2 | what-is-gws | The Reveal | `動態讀 API` | `gws 一個 CLI 接管 Drive、Gmail、Sheets，讀 Discovery 動態列能力` | `Google 一推就能用` |
| 3 | local-folder | The Path | `素材放本地` | `把 requirements.md 丟進 freelance/，Claude Code 就有 context` | `丟資料夾` |
| 4 | claude-design | The Chasm | `先設計題目` | `不是直接產表單，先讀需求寫成 20 題分四段的 markdown` | `20 題 / 4 段` |
| 5 | one-sentence | The Collision | `一句話送出` | `「use gws to create the google form」AI 自動跑 create + batchUpdate` | `2 條指令` |
| 6 | universal | The Scale | `forms 換 docs` | — (closing insight, exempt) | `素材→Workspace` |

Slides 1 and 6 are exempt from the key-point rule (cover hooks, closing lands the universal pattern). Slides 2–5 carry all three text tiers.

Archetype rhythm (Spotlight → Reveal → Path → Chasm → Collision → Scale) — every adjacent pair differs.

Read the key points cold:
- 2: names the actual mechanism (Discovery Service, dynamic).
- 3: states the concrete file (requirements.md) and the concrete folder (freelance/).
- 4: states the concrete output (20 題, 4 段) and the order (read needs first, then design).
- 5: names the literal sentence and the two CLI calls (create + batchUpdate) that AI runs.

Each is a complete claim, not a slogan.
